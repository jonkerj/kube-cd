apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: backup
  namespace: velero
spec:
  schedule: '@every 24h'
  template:
    storageLocation: minio-neon
    includeClusterResources: true
    ttl: 732h
